<h1>New story</h1>

<% form_for(@story) do |f| %>
  <%= f.error_messages %>

  <%= render "form_fields", :f => f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', stories_path %>

<%= render "generator" %>